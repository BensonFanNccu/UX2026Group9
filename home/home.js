/* =============================================================
   home.js — 首頁專屬腳本
   目前首頁無額外互動（nav/footer 由 shared/layout.js 注入）。
   預留此檔給日後首頁的互動（例如數據動畫、輪播等）。
   ============================================================= */
(function () {
  'use strict';
  // 預留：首頁互動寫在這裡
})();
