# images/

放置作品集網站要用的圖片：App 截圖、流程圖、線框稿、persona 圖、主視覺等。

- 命名建議：`app-chat.png`、`flow-overview.png`、`persona-1.png`…
- 引用時用相對路徑，例如 `images/app-chat.png`，並加 `loading="lazy"`。
- 與 `rehab-app/`、`rehab-app-src/` 內的圖無關，那是 App 自己的資源。
